name: mad
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  flutter_stripe: ^10.1.1
  http: ^1.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  geolocator: ^11.0.0
  location: ^5.0.3
  geocoding: ^2.1.1
  google_generative_ai: ^0.4.0
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  fl_chart: ^0.66.0
  flutter_local_notifications: ^17.1.2
  timezone: ^0.9.2
  google_maps_flutter: ^2.5.3
  mobile_scanner: ^4.0.0 # Added mobile_scanner dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/myqr.jpeg
